---
name: nodefony-add-realtime-channel
description: >
  Ajoute un flux temps réel à une application Nodefony par la bonne couche — un `RealtimeController` et ses décorateurs de canal — au lieu de recomposer un WebSocket à la main.
metadata:
  nodefony-source-package: "@nodefony/devkit"
---

# nodefony-add-realtime-channel

> Ce fichier est un **pointeur**, posé par `nodefony ai:sync`. Le contenu vit
> dans le paquet `@nodefony/devkit` et se met à jour par `npm update` —
> ne l'édite pas ici, l'édition serait écrasée et ne profiterait à personne.

**Lis maintenant `node_modules/@nodefony/devkit/skills/nodefony-add-realtime-channel/SKILL.md`** : c'est la version qui correspond à la
version du framework installée dans ce projet.
