---
name: nodefony-dev
description: >
  Conduit une tâche de développement de bout en bout dans une application Nodefony — comprendre le code en place, choisir la bonne façade, générer plutôt qu'écrire à la main, retrouver la référence installée qu'une recherche ordinaire ne voit pas, puis prouver que c'est fait — et se charge AVANT la première modification, quelle que soit la tâche.
metadata:
  nodefony-source-package: "@nodefony/devkit"
---

# nodefony-dev

> Ce fichier est un **pointeur**, posé par `nodefony ai:sync`. Le contenu vit
> dans le paquet `@nodefony/devkit` et se met à jour par `npm update` —
> ne l'édite pas ici, l'édition serait écrasée et ne profiterait à personne.

**Lis maintenant `node_modules/@nodefony/devkit/skills/nodefony-dev/SKILL.md`** : c'est la version qui correspond à la
version du framework installée dans ce projet.
