---
name: nodefony-add-service
description: >
  Crée un service injectable dans une application Nodefony par `nodefony create service`, et le fait entrer dans le conteneur — la moitié qu'on oublie.
metadata:
  nodefony-source-package: "@nodefony/devkit"
---

# nodefony-add-service

> Ce fichier est un **pointeur**, posé par `nodefony ai:sync`. Le contenu vit
> dans le paquet `@nodefony/devkit` et se met à jour par `npm update` —
> ne l'édite pas ici, l'édition serait écrasée et ne profiterait à personne.

**Lis maintenant `node_modules/@nodefony/devkit/skills/nodefony-add-service/SKILL.md`** : c'est la version qui correspond à la
version du framework installée dans ce projet.
