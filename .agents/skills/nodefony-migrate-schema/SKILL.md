---
name: nodefony-migrate-schema
description: >
  Fait évoluer le schéma d'une base Nodefony et le porte en production, par les commandes `orm:generate` et `orm:migrate` du framework — jamais par un `ALTER` écrit à la main ni par la suppression d'une base.
metadata:
  nodefony-source-package: "@nodefony/devkit"
---

# nodefony-migrate-schema

> Ce fichier est un **pointeur**, posé par `nodefony ai:sync`. Le contenu vit
> dans le paquet `@nodefony/devkit` et se met à jour par `npm update` —
> ne l'édite pas ici, l'édition serait écrasée et ne profiterait à personne.

**Lis maintenant `node_modules/@nodefony/devkit/skills/nodefony-migrate-schema/SKILL.md`** : c'est la version qui correspond à la
version du framework installée dans ce projet.
