---
name: nodefony-add-crud
description: >
  Crée une ressource complète dans une application Nodefony — table, schémas de validation, service CRUD, controller REST+WebSocket et tests — par le générateur `nodefony create entity`, au lieu de l'écrire à la main.
metadata:
  nodefony-source-package: "@nodefony/devkit"
---

# nodefony-add-crud

> Ce fichier est un **pointeur**, posé par `nodefony ai:sync`. Le contenu vit
> dans le paquet `@nodefony/devkit` et se met à jour par `npm update` —
> ne l'édite pas ici, l'édition serait écrasée et ne profiterait à personne.

**Lis maintenant `node_modules/@nodefony/devkit/skills/nodefony-add-crud/SKILL.md`** : c'est la version qui correspond à la
version du framework installée dans ce projet.
